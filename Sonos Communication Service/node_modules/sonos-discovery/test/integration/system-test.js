'use strict';

var expect = require('chai').expect;
var sinon = require('sinon');
require('chai').use(require('sinon-chai'));

var SonosSystem = require('../../lib/SonosSystem');

describe.only('System test', function () {
  var system = void 0;

  before(function (done) {
    this.timeout(20000);
    system = new SonosSystem();
    system.on('topology-change', function () {
      return done();
    });
  });

  after(function () {
    //system.dispose();
  });

  it('Has zones', function () {
    expect(system.zones).not.empty;
  });

  // it('Pauses', (done) => {
  //   let player = system.getPlayer('Office');
  //   player.coordinator.pause()
  //     .then(() => {
  //       return player.coordinator.play();
  //     })
  //     .then(() => {
  //       done();
  //     });
  // });

  // it('Changes subGain', () => {
  //   let player = system.getPlayer('TV Room');
  //   return player.sub.setGain(10);
  // });
});